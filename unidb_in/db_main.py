import sqlite3
import pandas as pd

def handle_df(IEC61850Name, RussianName, dfs):
    pass
    # Подключение к базе данных SQLite
    #conn = sqlite3.connect('database.db')
    #df_info.to_sql('table1', conn, index=False, if_exists='replace')
    #df.to_sql('table2', conn, index=False, if_exists='replace')
    # Закрытие соединения с базой данных
    #conn.close()
    #conn = sqlite3.connect('unidb.db')
    


